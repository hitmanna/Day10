import Insta from "../Insta/Insta";

const Main = (addNewPost) => {

console.log(addNewPost)

    return (

        <div>
            < Insta addNewPost = {addNewPost} />
        </div>
    )
}

export {
    Main
}